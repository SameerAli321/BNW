/**
 * Deprecated: the access token is no longer persisted in sessionStorage (see utils.ts) — it is
 * kept in memory only, per the project guide's security note. This key is unused; kept in case
 * any leftover demo code still imports it.
 */
export const JWT_STORAGE_KEY = 'jwt_access_token';
